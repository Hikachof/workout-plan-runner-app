# Workout Plan Runner

PC/Codexが作成した `workout_plan.json` を読み込み、トレーニング実行と `workout_history.json` の書き出しを行うiPhone向けPWAです。

## 使い方

1. GitHub PagesのURLをiPhone Safariで開く。
2. 共有メニューから「ホーム画面に追加」を選ぶ。
3. PWAを起動し、「ファイル」から計画JSONを読み込む。
4. メニューを実行して、実績JSONを書き出す。

このリポジトリには個人の計画・履歴データは含めません。
